'use strict';

describe('candyApp.login module', function() {

  beforeEach(module('candyApp.login'));

  describe('manage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var manageCtrl = $controller('loginCtrl');
      expect(manageCtrl).toBeDefined();
    }));

  });
});