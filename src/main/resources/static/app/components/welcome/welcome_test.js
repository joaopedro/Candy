'use strict';

describe('candyApp.welcome module', function() {

  beforeEach(module('candyApp.welcome'));

  describe('welcome controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var entityCtrl = $controller('WelcomeCtrl');
      expect(entityCtrl).toBeDefined();
    }));

  });
});