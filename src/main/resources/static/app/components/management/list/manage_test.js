'use strict';

describe('candyApp.manage.list module', function() {

  beforeEach(module('candyApp.manage.list'));

  describe('manage list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listManageCtrl = $controller('listManageCtrl');
      expect(listManageCtrl).toBeDefined();
    }));

  });
});