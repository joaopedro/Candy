'use strict';

describe('candyApp.manage.edit module', function() {

  beforeEach(module('candyApp.manage.edit'));

  describe('manage edit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editManageCtrl = $controller('editManageCtrl');
      expect(editManageCtrl).toBeDefined();
    }));

  });
});