'use strict';

describe('candyApp.manage module', function() {

  beforeEach(module('candyApp.manage'));

  describe('manage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var manageCtrl = $controller('manageCtrl');
      expect(manageCtrl).toBeDefined();
    }));

  });
});