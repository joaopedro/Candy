'use strict';

describe('candyApp.entity module', function() {

  beforeEach(module('candyApp.entity'));

  describe('entity controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var entityCtrl = $controller('entityCtrl');
      expect(entityCtrl).toBeDefined();
    }));

  });
});